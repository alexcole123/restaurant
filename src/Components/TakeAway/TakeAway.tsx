import css from "./TakeAway.module.css";

export function TakeAway(): JSX.Element {
    return (
        <div>
            <p className={css.CoolPar}>Our take-away is open all day.</p>
        </div>
    );
}
